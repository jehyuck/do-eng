param([Parameter(Mandatory)][ValidateSet('BASELINE','REMEDIATION')][string]$Condition,[Parameter(Mandatory)][ValidateSet('001','002','003')][string]$RunIndex,[string]$RunDate='PLAN-DATE',[ValidateSet('PLAN','EXECUTE')][string]$ExecutionMode='PLAN')
$ErrorActionPreference='Stop'
. (Join-Path $PSScriptRoot 'experiment-1-23-collector-lifecycle.ps1')
. (Join-Path $PSScriptRoot 'experiment-1-23-collector-coverage.ps1')
. (Join-Path $PSScriptRoot 'experiment-1-23-artifact-contract.ps1')
. (Join-Path $PSScriptRoot 'experiment-1-23-container-stop.ps1')
. (Join-Path $PSScriptRoot 'experiment-1-23-native-log-capture.ps1')
$root=(Resolve-Path (Join-Path $PSScriptRoot '..\..')).Path;$id="RUN-$RunDate-EXP123-$Condition-$RunIndex";$result=Join-Path $root 'backend\experiments\results\experiment-1-23';$target=Join-Path $result "core\$id";$staging=Join-Path $result "collector-staging\$id";$app='doeng-flux-exp119-fresh-first-20260803:latest';$appId='sha256:c2878d2847f80f3396a5cee5f02f1ec1ca3f7c14ceb8f49ea610d6e5fb6d9168';$mock='doeng-exp119-mock-frozen-20260803:latest';$mockId='sha256:2492f942c3931aa607293cf3da940e0e5aac0cfae91cbfe0ea88a893f0829ac1';$policy=if($Condition-eq'BASELINE'){@{leasingStrategy='FIFO';maxIdleTimeMs=0;evictionIntervalMs=0}}else{@{leasingStrategy='LIFO';maxIdleTimeMs=3000;evictionIntervalMs=1000}}
$composeFiles=@('backend\docker-compose.experiment.yaml','backend\docker-compose.app-instance-t3-medium.yaml','backend\docker-compose.mock-headroom-4cpu.yaml','backend\docker-compose.http-pool-400.yaml','backend\docker-compose.mock-memory-headroom.yaml','backend\docker-compose.experiment-1-12-connection-attribution.yaml','backend\docker-compose.experiment-1-19-fresh-first-lifecycle.yaml');$composeArgs=@();foreach($f in $composeFiles){$composeArgs+=@('-f',(Join-Path $root $f))};$project='doeng-exp123-core'
function Write-Json($v,$p){New-Item -ItemType Directory -Force -Path (Split-Path $p)|Out-Null;$v|ConvertTo-Json -Depth 16|Set-Content $p -Encoding UTF8}
$manifest=[ordered]@{experiment='Experiment 1-23';runId=$id;runDate=$RunDate;condition=$Condition;executionMode=$ExecutionMode;applicationImageId=$appId;mockImageId=$mockId;policy=$policy;collectorMode='CORE_BOUND_STOP_SIGNAL';collector=@{intervalMilliseconds=1000;readinessTimeoutSeconds=10;postCoreSampleTimeoutSeconds=10;gracefulStopTimeoutSeconds=10;maxDurationSeconds=300};workload=@{vu=200;durationSeconds=105;aiDelayMs=2000;storageDelayMs=100;timeoutMs=10000;drainSeconds=30;appCpu=2;appMemory='3GiB';poolMax=400;pending=800;dbPool=10;admission=320}}
New-Item -ItemType Directory -Force -Path (Join-Path $result 'plan')|Out-Null;Write-Json $manifest (Join-Path $result "plan\$Condition-$RunIndex-command-manifest.json");if($ExecutionMode-eq'PLAN'){"PLAN READY: $id";exit 0};if($RunDate-notmatch'^\d{8}$'){throw 'EXECUTE requires YYYYMMDD'};foreach($p in @($target,$staging)){if(Test-Path $p){throw "Artifact collision: $p"}};if((docker image inspect --format '{{.Id}}' $app).Trim()-ne$appId){throw 'FROZEN_APPLICATION_IMAGE_MISMATCH'};if((docker image inspect --format '{{.Id}}' $mock).Trim()-ne$mockId){throw 'FROZEN_MOCK_IMAGE_MISMATCH'};New-Item -ItemType Directory -Force -Path $target,$staging|Out-Null;New-Item -ItemType File -Path (Join-Path $target 'RUNNING')|Out-Null
$previous=@{};foreach($pair in @{'DOENG_EXTERNAL_LEASING_STRATEGY'=$policy.leasingStrategy;'DOENG_EXTERNAL_MAX_IDLE_TIME_MS'=$policy.maxIdleTimeMs;'DOENG_EXTERNAL_EVICTION_INTERVAL_MS'=$policy.evictionIntervalMs}.GetEnumerator()){$previous[$pair.Key]=[Environment]::GetEnvironmentVariable($pair.Key,'Process');[Environment]::SetEnvironmentVariable($pair.Key,[string]$pair.Value,'Process')};$collector=$null;$failure=$null;$steps=@()
try{docker compose -p $project @composeArgs up -d --force-recreate --no-build mariadb experiment-mock flux-corrected;if($LASTEXITCODE){throw 'Fresh recreate failed'};$steps+='fresh recreate';& powershell.exe -NoProfile -ExecutionPolicy Bypass -File (Join-Path $PSScriptRoot 'run-isolated-vu-success-smoke.ps1') -RunId "SMOKE-$RunDate-EXP123-$Condition-$RunIndex" -Implementation "Exp123-$Condition-warmup" -TargetUrl http://127.0.0.1:8001/game/face -ServerService flux-corrected -ComposeProject $project -ComposeFiles ($composeFiles -join ',') -ActiveMissions 20 -AiDelayMs 2000 -StorageDelayMs 1000 -IntervalMs 1000 -DurationMs 5000 -RequestTimeoutMs 10000 -TargetP95Ms 10000 -EnableObservability 0 -OutcomeMode controlled-admission -AccountingMode corrected -DrainObservationSeconds 10;if($LASTEXITCODE){throw 'Warm-up failed'};$steps+='warm-up';New-Item -ItemType Directory -Force -Path $staging|Out-Null;$collectorStarted=(Get-Date).ToUniversalTime().ToString('o');$jsonl=Join-Path $staging 'pool-metrics.jsonl';$summary=Join-Path $staging 'pool-metrics.jsonl.summary.json';$signal=Join-Path $staging 'STOP-COLLECTOR';$collector=Start-Process powershell.exe -ArgumentList @('-NoProfile','-ExecutionPolicy','Bypass','-File',(Join-Path $PSScriptRoot 'collect-diagnostic-pool-until-stop.ps1'),'-ManagementUrl','http://127.0.0.1:9001','-OutputPath',$jsonl,'-SummaryPath',$summary,'-StopSignalPath',$signal,'-IntervalMilliseconds','1000','-MaxDurationSeconds','300') -WindowStyle Hidden -PassThru;$ready=Wait-Exp123CollectorReady $collector $jsonl 10;$steps+='collector readiness';$timeline=[ordered]@{collectorProcessStartedAt=$collectorStarted;collectorFirstValidSampleAt=$ready.collectorFirstValidSampleAt;coreInvocationStartedAt=$null;coreInvocationCompletedAt=$null;collectorProcessCompletedAt=$null;applicationStopStartedAt=$null;applicationStopCompletedAt=$null;applicationLogCaptureStartedAt=$null;applicationLogCaptureCompletedAt=$null};Write-Json $timeline (Join-Path $target 'provenance/execution-timeline.json');$coreStarted=(Get-Date).ToUniversalTime().ToString('o');$timeline.coreInvocationStartedAt=$coreStarted;Write-Json $timeline (Join-Path $target 'provenance/execution-timeline.json');$legacy=Join-Path $root "experiment\results\$id";& powershell.exe -NoProfile -ExecutionPolicy Bypass -File (Join-Path $PSScriptRoot 'run-isolated-vu-success-smoke.ps1') -RunId $id -Implementation "Exp123-$Condition-core" -TargetUrl http://127.0.0.1:8001/game/face -ServerService flux-corrected -ComposeProject $project -ComposeFiles ($composeFiles -join ',') -ActiveMissions 200 -InitialActiveUsers 200 -ActivationStepUsers 200 -AiDelayMs 2000 -StorageDelayMs 100 -IntervalMs 1000 -DurationMs 105000 -RequestTimeoutMs 10000 -TargetP95Ms 10000 -EnableObservability 1 -EnableJfr 0 -EnableContainerMonitor 1 -SkipApplicationSnapshot 1 -SkipMockMetrics 1 -OutcomeMode controlled-admission -AccountingMode corrected -DrainObservationSeconds 30;$coreExit=$LASTEXITCODE;$coreCompleted=(Get-Date).ToUniversalTime().ToString('o');$timeline.coreInvocationCompletedAt=$coreCompleted;Write-Json $timeline (Join-Path $target 'provenance/execution-timeline.json');$steps+='core invocation';if($coreExit){throw 'CORE'};$cover=Wait-Exp123CollectorCoversCoreEnd $jsonl $coreCompleted 10;$confirmed=(Get-Date).ToUniversalTime().ToString('o');$tmp=Join-Path $staging 'STOP-COLLECTOR.tmp';New-Item -ItemType File $tmp|Out-Null;Move-Item -Force $tmp $signal;$issued=(Get-Date).ToUniversalTime().ToString('o');$collector.WaitForExit(10000)|Out-Null;if(-not$collector.HasExited){throw 'COLLECTOR_STOP_TIMEOUT'};$collectorCompleted=(Get-Date).ToUniversalTime().ToString('o');$steps+='collector graceful exit';Copy-Item $jsonl (Join-Path $target 'pool/pool-metrics.jsonl') -Force;Copy-Item ($jsonl+'.summary.json') (Join-Path $target 'pool/pool-metrics.jsonl.summary.json') -Force;$lifecycle=New-Exp123CollectorLifecycle $id 'CORE_BOUND_STOP_SIGNAL' 1000 10 10 10 300 $collectorStarted $ready.collectorFirstValidSampleAt $coreStarted $coreCompleted $cover $confirmed $issued $collectorCompleted $true $collector.ExitCode $false $false ([int64]([DateTimeOffset]::Parse($collectorCompleted)-[DateTimeOffset]::Parse($collectorStarted)).TotalMilliseconds) 'COLLECTOR_LIFECYCLE_PASSED' $null;Write-Json $lifecycle (Join-Path $target 'pool/collector-lifecycle.json');$steps+='collector coverage';if(Test-Path $legacy){Copy-Item $legacy\* $target -Recurse -Force};$steps+='artifact copy'}catch{$failure=$_}finally{if($collector-and-not$collector.HasExited){$collector.Kill()};docker compose -p $project @composeArgs down --remove-orphans|Out-Null;foreach($pair in $previous.GetEnumerator()){[Environment]::SetEnvironmentVariable($pair.Key,$pair.Value,'Process')}}
if($failure){Set-Exp123Terminal $target 'EXECUTION_FAILED';Write-Json ([ordered]@{runId=$id;executionStatus='EXECUTION_FAILED';artifactValidation='FAILED';failureDomain=if($failure.Exception.Message-match'COLLECTOR'){'COLLECTOR'}elseif($failure.Exception.Message-match'CORE'){'CORE'}else{'EXECUTION_FAILED'};errorMessage=$failure.Exception.Message;completedSteps=$steps}) (Join-Path $target 'failure-summary.json');Write-Json ([ordered]@{runId=$id;executionStatus='EXECUTION_FAILED';artifactValidation='FAILED';completedSteps=$steps}) (Join-Path $target 'execution-summary.json');throw $failure}
Set-Exp123Terminal $target 'COMPLETED';Write-Json ([ordered]@{runId=$id;executionStatus='COMPLETED';artifactValidation='PASSED';completedSteps=$steps}) (Join-Path $target 'execution-summary.json')
