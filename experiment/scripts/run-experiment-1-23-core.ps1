param([Parameter(Mandatory)][ValidateSet('BASELINE','REMEDIATION')][string]$Condition,[Parameter(Mandatory)][ValidateSet('001','002','003')][string]$RunIndex,[string]$RunDate='PLAN-DATE',[ValidateSet('PLAN','EXECUTE')][string]$ExecutionMode='PLAN')
$ErrorActionPreference='Stop';$root=(Resolve-Path (Join-Path $PSScriptRoot '..\..')).Path
. (Join-Path $PSScriptRoot 'experiment-1-23-collector-lifecycle.ps1');. (Join-Path $PSScriptRoot 'experiment-1-23-collector-coverage.ps1');. (Join-Path $PSScriptRoot 'experiment-1-23-artifact-contract.ps1');. (Join-Path $PSScriptRoot 'experiment-1-23-container-stop.ps1');. (Join-Path $PSScriptRoot 'experiment-1-23-native-log-capture.ps1')
$id="RUN-$RunDate-EXP123-$Condition-$RunIndex";$result=Join-Path $root 'backend\experiments\results\experiment-1-23';$target=Join-Path $result "core\$id";$staging=Join-Path $result "collector-staging\$id";$legacy=Join-Path $root "experiment\results\$id";$project='doeng-exp123-core'
$app='doeng-flux-exp119-fresh-first-20260803:latest';$appId='sha256:c2878d2847f80f3396a5cee5f02f1ec1ca3f7c14ceb8f49ea610d6e5fb6d9168';$mock='doeng-exp119-mock-frozen-20260803:latest';$mockId='sha256:2492f942c3931aa607293cf3da940e0e5aac0cfae91cbfe0ea88a893f0829ac1'
$files=@('backend\docker-compose.experiment.yaml','backend\docker-compose.app-instance-t3-medium.yaml','backend\docker-compose.mock-headroom-4cpu.yaml','backend\docker-compose.http-pool-400.yaml','backend\docker-compose.mock-memory-headroom.yaml','backend\docker-compose.experiment-1-12-connection-attribution.yaml','backend\docker-compose.experiment-1-19-fresh-first-lifecycle.yaml');$compose=@();foreach($f in $files){$compose+=@('-f',(Join-Path $root $f))};$policy=if($Condition-eq'BASELINE'){@{leasingStrategy='FIFO';maxIdleTimeMs=0;evictionIntervalMs=0}}else{@{leasingStrategy='LIFO';maxIdleTimeMs=3000;evictionIntervalMs=1000}}
function W($v,$p){New-Item -ItemType Directory -Force -Path (Split-Path $p)|Out-Null;$v|ConvertTo-Json -Depth 20|Set-Content -LiteralPath $p -Encoding UTF8};function ImageId($t){((& docker image inspect --format '{{.Id}}' $t).Trim())};function ComposeSha($f){(Get-FileHash -LiteralPath (Join-Path $root $f) -Algorithm SHA256).Hash.ToLowerInvariant()}
$manifest=[ordered]@{experiment='Experiment 1-23';runId=$id;runDate=$RunDate;condition=$Condition;executionMode=$ExecutionMode;applicationImageTag=$app;applicationImageId=$appId;mockImageTag=$mock;mockImageId=$mockId;policy=$policy;composeFiles=$files;workload=@{vu=200;durationSeconds=105;aiDelayMs=2000;storageDelayMs=100;timeoutMs=10000;frameIntervalSeconds=1;drainSeconds=30;appCpu=2;appMemory='3GiB';httpPool=400;pending=800;dbPool=10;admission=320};collector=@{mode='CORE_BOUND_STOP_SIGNAL';intervalMilliseconds=1000;readinessTimeoutSeconds=10;postCoreSampleTimeoutSeconds=10;gracefulStopTimeoutSeconds=10;maxDurationSeconds=300}}
New-Item -ItemType Directory -Force (Join-Path $result 'plan')|Out-Null;W $manifest (Join-Path $result "plan\$Condition-$RunIndex-command-manifest.json");if($ExecutionMode-eq'PLAN'){"PLAN READY: $id";exit 0};if($RunDate-notmatch'^\d{8}$'){throw 'EXECUTE requires YYYYMMDD'};if((ImageId $app)-ne$appId -or(ImageId $mock)-ne$mockId){throw 'FROZEN_IMAGE_MISMATCH'};foreach($p in @($target,$staging)){if(Test-Path $p){throw "ARTIFACT_COLLISION: $p"}};New-Item -ItemType Directory -Force $target,$staging|Out-Null;New-Item -ItemType File (Join-Path $target 'RUNNING')|Out-Null;W $manifest (Join-Path $target 'run-config.json')
$old=@{};foreach($e in @{'DOENG_EXP119_APP_IMAGE'=$app;'DOENG_EXP119_MOCK_IMAGE'=$mock;'DOENG_EXTERNAL_LEASING_STRATEGY'=$policy.leasingStrategy;'DOENG_EXTERNAL_MAX_IDLE_TIME_MS'=$policy.maxIdleTimeMs;'DOENG_EXTERNAL_EVICTION_INTERVAL_MS'=$policy.evictionIntervalMs}.GetEnumerator()){$old[$e.Key]=[Environment]::GetEnvironmentVariable($e.Key,'Process');[Environment]::SetEnvironmentVariable($e.Key,[string]$e.Value,'Process')}
$collector=$null;$failure=$null;$terminal=$false;$steps=[Collections.Generic.List[string]]::new();$tlPath=Join-Path $target 'provenance/execution-timeline.json';$tl=[ordered]@{collectorProcessStartedAt=$null;collectorFirstValidSampleAt=$null;coreInvocationStartedAt=$null;coreInvocationCompletedAt=$null;collectorFirstSampleCoveringCoreEndAt=$null;postCoreCoverageConfirmedAt=$null;collectorStopSignalIssuedAt=$null;collectorProcessCompletedAt=$null;applicationStopStartedAt=$null;applicationStopCompletedAt=$null;applicationLogCaptureStartedAt=$null;applicationLogCaptureCompletedAt=$null;artifactValidationCompletedAt=$null;finalizationCompletedAt=$null}
$manifest.composeFiles=@($files|ForEach-Object{[ordered]@{path=$_;sha256=(ComposeSha $_)}});W $manifest (Join-Path $target 'run-config.json')
try {
 docker compose -p $project @compose up -d --force-recreate --no-build mariadb experiment-mock flux-corrected;if($LASTEXITCODE){throw 'FRESH_RECREATE_FAILED'};$steps.Add('fresh recreate')
 & powershell -NoProfile -ExecutionPolicy Bypass -File (Join-Path $PSScriptRoot 'run-isolated-vu-success-smoke.ps1') -RunId "SMOKE-$RunDate-EXP123-$Condition-$RunIndex" -Implementation "Exp123-$Condition-warmup" -TargetUrl http://127.0.0.1:8001/game/face -ServerService flux-corrected -ComposeProject $project -ComposeFiles ($files -join ',') -ActiveMissions 20 -AiDelayMs 2000 -StorageDelayMs 100 -IntervalMs 1000 -DurationMs 5000 -RequestTimeoutMs 10000 -TargetP95Ms 10000 -EnableObservability 0 -OutcomeMode controlled-admission -AccountingMode corrected -DrainObservationSeconds 10;if($LASTEXITCODE){throw 'WARMUP_FAILED'};$steps.Add('warm-up')
 $jsonl=Join-Path $staging 'pool-metrics.jsonl';$sum=$jsonl+'.summary.json';$signal=Join-Path $staging 'STOP-COLLECTOR';$tl.collectorProcessStartedAt=(Get-Date).ToUniversalTime().ToString('o');$collector=Start-Process powershell.exe -WindowStyle Hidden -PassThru -ArgumentList @('-NoProfile','-ExecutionPolicy','Bypass','-File',(Join-Path $PSScriptRoot 'collect-diagnostic-pool-until-stop.ps1'),'-ManagementUrl','http://127.0.0.1:9001','-OutputPath',$jsonl,'-SummaryPath',$sum,'-StopSignalPath',$signal,'-IntervalMilliseconds','1000','-MaxDurationSeconds','300');$ready=Wait-Exp123CollectorReady $collector $jsonl 10;$tl.collectorFirstValidSampleAt=$ready.collectorFirstValidSampleAt;W $tl $tlPath;$steps.Add('collector readiness')
 $tl.coreInvocationStartedAt=(Get-Date).ToUniversalTime().ToString('o');W $tl $tlPath;& powershell -NoProfile -ExecutionPolicy Bypass -File (Join-Path $PSScriptRoot 'run-isolated-vu-success-smoke.ps1') -RunId $id -Implementation "Exp123-$Condition-core" -TargetUrl http://127.0.0.1:8001/game/face -ServerService flux-corrected -ComposeProject $project -ComposeFiles ($files -join ',') -ActiveMissions 200 -InitialActiveUsers 200 -ActivationStepUsers 200 -AiDelayMs 2000 -StorageDelayMs 100 -IntervalMs 1000 -DurationMs 105000 -RequestTimeoutMs 10000 -TargetP95Ms 10000 -EnableObservability 1 -EnableJfr 0 -EnableContainerMonitor 1 -SkipApplicationSnapshot 1 -SkipMockMetrics 1 -OutcomeMode controlled-admission -AccountingMode corrected -DrainObservationSeconds 30;$coreExit=$LASTEXITCODE;$tl.coreInvocationCompletedAt=(Get-Date).ToUniversalTime().ToString('o');W $tl $tlPath;if($coreExit){throw 'CORE_PROCESS_FAILED'};$steps.Add('core invocation')
 $coverEnd=Wait-Exp123CollectorCoversCoreEnd $jsonl $tl.coreInvocationCompletedAt 10;$tl.collectorFirstSampleCoveringCoreEndAt=$coverEnd;$tl.postCoreCoverageConfirmedAt=(Get-Date).ToUniversalTime().ToString('o');New-Item -ItemType File -Path ($signal+'.tmp')|Out-Null;Move-Item -Force ($signal+'.tmp') $signal;$tl.collectorStopSignalIssuedAt=(Get-Date).ToUniversalTime().ToString('o');if(-not$collector.WaitForExit(10000)){throw 'COLLECTOR_STOP_TIMEOUT'};$tl.collectorProcessCompletedAt=(Get-Date).ToUniversalTime().ToString('o');$summaryObj=Get-Content $sum -Raw|ConvertFrom-Json;if($summaryObj.collectorStatus-ne'COLLECTOR_STOPPED_BY_SIGNAL'-or$summaryObj.stopSignalObserved-ne$true-or[int]$summaryObj.failures-ne0-or[int]$collector.ExitCode-ne0){throw 'COLLECTOR_SUMMARY_INVALID'};W $tl $tlPath;$steps.Add('collector lifecycle')
 New-Item -ItemType Directory -Force (Join-Path $target 'pool')|Out-Null;Copy-Item $jsonl (Join-Path $target 'pool/pool-metrics.jsonl');Copy-Item $sum (Join-Path $target 'pool/pool-metrics.jsonl.summary.json');$life=New-Exp123CollectorLifecycle $id 'CORE_BOUND_STOP_SIGNAL' 1000 10 10 10 300 $tl.collectorProcessStartedAt $tl.collectorFirstValidSampleAt $tl.coreInvocationStartedAt $tl.coreInvocationCompletedAt $coverEnd $tl.postCoreCoverageConfirmedAt $tl.collectorStopSignalIssuedAt $tl.collectorProcessCompletedAt $true $collector.ExitCode $false $false ([int64]([DateTimeOffset]::Parse($tl.collectorProcessCompletedAt)-[DateTimeOffset]::Parse($tl.collectorProcessStartedAt)).TotalMilliseconds) 'COLLECTOR_LIFECYCLE_PASSED' $null $summaryObj.collectorStatus ([int]$summaryObj.failures) ([bool]$summaryObj.stopSignalObserved);W $life (Join-Path $target 'pool/collector-lifecycle.json');$coverage=New-Exp123CollectorCoverage $id $jsonl $sum 300 1000 $tl.collectorProcessStartedAt $tl.coreInvocationStartedAt $tl.coreInvocationCompletedAt $tl.collectorProcessCompletedAt;W $coverage (Join-Path $target 'pool/collector-coverage.json');Assert-Exp123CollectorCoverage $target;$steps.Add('collector coverage')
 $container=(docker compose -p $project @compose ps -q flux-corrected).Trim();if([string]::IsNullOrWhiteSpace($container)){throw 'APPLICATION_CONTAINER_UNAVAILABLE'};$stop=Invoke-Exp123ContainerStop @{Project=$project;ComposeFiles=($files|ForEach-Object{Join-Path $root $_});Service='flux-corrected';RunId=$id;MetadataPath=(Join-Path $target 'application/container-stop.json');ContainerId=$container;GracePeriodSeconds=15;ProcessTimeoutSeconds=30};$tl.applicationStopStartedAt=$stop.startedAt;$tl.applicationStopCompletedAt=$stop.completedAt;W $tl $tlPath;$cap=Invoke-Exp123PostStopLogCapture @{RunId=$id;ContainerId=$container;ApplicationDirectory=(Join-Path $target 'application');TimeoutSeconds=30;ContainerStateAtCapture=$stop.stateAfter};$tl.applicationLogCaptureStartedAt=$cap.startedAt;$tl.applicationLogCaptureCompletedAt=$cap.completedAt;W $tl $tlPath;$steps.Add('application stop');$steps.Add('post-stop log capture')
 if(-not(Test-Path $legacy)){throw 'LEGACY_ARTIFACTS_MISSING'};foreach($name in @('client-results.json','client-progress.jsonl','database-metrics.jsonl','container-stats.jsonl','load-stop-mock-metrics.json','mock-drain-summary.json','verification-summary.json')){$src=Join-Path $legacy $name;if(-not(Test-Path $src)){throw "LEGACY_ARTIFACT_MISSING: $name"};$dest=switch($name){'database-metrics.jsonl'{'database/database-metrics.jsonl'}'container-stats.jsonl'{'container/container-stats.jsonl'}'load-stop-mock-metrics.json'{'mock/load-stop-mock-metrics.json'}'mock-drain-summary.json'{'drain/mock-drain-summary.json'}default{$name}};New-Item -ItemType Directory -Force (Split-Path (Join-Path $target $dest))|Out-Null;Copy-Item $src (Join-Path $target $dest) -Force}
 $runtime=[ordered]@{runtimeProvenance='ACTUAL';runId=$id;runDate=$RunDate;condition=$Condition;applicationImageTag=$app;applicationImageId=$appId;mockImageTag=$mock;mockImageId=$mockId;policy=$policy;collectorMode='CORE_BOUND_STOP_SIGNAL';collectorReadyAt=$ready.collectorReadyAt;collectorFirstValidSampleAt=$ready.collectorFirstValidSampleAt;composeFiles=@($files|ForEach-Object{[ordered]@{path=$_;sha256=(ComposeSha $_)}})};W $runtime (Join-Path $target 'provenance/runtime-provenance.json');$steps.Add('artifact copy')
 W ([ordered]@{runId=$id;executionStatus='FINALIZING';artifactValidation='PENDING';cleanupResult='NOT_ATTEMPTED';completedSteps=@($steps)}) (Join-Path $target 'execution-summary.json');Assert-Exp123StructuralArtifacts $target;$tl.artifactValidationCompletedAt=(Get-Date).ToUniversalTime().ToString('o');W $tl $tlPath;W ([ordered]@{runId=$id;executionStatus='COMPLETED';artifactValidation='PASSED';cleanupResult='NOT_ATTEMPTED';completedSteps=@($steps)}) (Join-Path $target 'execution-summary.json');$tl.finalizationCompletedAt=(Get-Date).ToUniversalTime().ToString('o');W $tl $tlPath;Assert-Exp123CommittedArtifacts $target
 docker compose -p $project @compose down --remove-orphans|Out-Null;if($LASTEXITCODE){throw 'CLEANUP_FAILED'};$left=(docker compose -p $project @compose ps -q).Trim();if(-not[string]::IsNullOrWhiteSpace($left)){throw 'CLEANUP_CONTAINERS_REMAIN'};W ([ordered]@{runId=$id;executionStatus='COMPLETED';artifactValidation='PASSED';cleanupResult='COMPLETED';completedSteps=@($steps)}) (Join-Path $target 'execution-summary.json');Assert-Exp123CommittedArtifacts $target;Set-Exp123Terminal $target 'COMPLETED';$terminal=$true
}catch{$failure=$_}finally{if($null-ne$collector -and -not$collector.HasExited){$collector.Kill()};if($null-ne$failure){docker compose -p $project @compose down --remove-orphans|Out-Null};foreach($e in $old.GetEnumerator()){[Environment]::SetEnvironmentVariable($e.Key,$e.Value,'Process')}}
if($failure){Set-Exp123Terminal $target 'EXECUTION_FAILED';W ([ordered]@{runId=$id;executionStatus='EXECUTION_FAILED';artifactValidation='FAILED';cleanupResult='FAILED_OR_NOT_ATTEMPTED';failureDomain=$failure.Exception.Message;completedSteps=@($steps)}) (Join-Path $target 'failure-summary.json');W ([ordered]@{runId=$id;executionStatus='EXECUTION_FAILED';artifactValidation='FAILED';cleanupResult='FAILED_OR_NOT_ATTEMPTED';completedSteps=@($steps)}) (Join-Path $target 'execution-summary.json');throw $failure}
